package pl.cwanix.opensun.agentserver.engine.experimental.maps.objects

import pl.cwanix.opensun.agentserver.engine.experimental.maps.structures.FieldInfoStructure
import spock.lang.Specification

class FieldInfoTest extends Specification {

    /*def "loader should find area chunk"() {
        given:
            def fieldInfoStructure = new FieldInfoStructure(fieldCode: 10001, path: 'Map\\Object\\Map_Tringel\\Map_Tringel.map')
            def fieldInfo = new FieldInfo(fieldInfoStructure)

            def result = false

        when:
            for (int i = 0; i < 230000 && !result; i++) {
                try {
                    fieldInfo.load('c:/dev/data', i)
                    result = true
                    println 'znaleziono dla ' + i
                } catch (Exception e) {

                }
            }

        then:
            result
    }*/
}
